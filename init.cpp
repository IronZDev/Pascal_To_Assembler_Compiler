#include "global.hpp"
#include "parser.hpp"
struct entry keywords[] = { 
  //{"div", div}, 
  // {"mod", MOD}, 
  {"0", 0} 
};

void init () 
{
  // struct entry *p;
  // for (p = keywords; p->token; p++)
  //   insert (p->lexptr, p->token);
}


